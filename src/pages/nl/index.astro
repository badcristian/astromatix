---
import Base from '../../layouts/Base.astro';
---

<Base title="Jobmatix">
  {/* Body sections land here next. This shell exists so the header and footer
      can be verified against the original before any page content is built. */}
  <div class="container-site py-24">
    <p class="text-navy/50">Body sections pending.</p>
  </div>
</Base>
