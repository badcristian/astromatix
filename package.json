{
  "name": "jobmatix-astro",
  "type": "module",
  "version": "0.0.1",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "astro": "astro",
    "capture:original": "node scripts/capture.mjs --target original",
    "capture:rebuild": "node scripts/capture.mjs --target rebuild",
    "deploy": "astro build && wrangler deploy"
  },
  "dependencies": {
    "@tailwindcss/vite": "^4.3.3",
    "astro": "^7.1.2",
    "tailwindcss": "^4.3.3"
  },
  "devDependencies": {
    "odiff-bin": "^4.3.8",
    "playwright": "^1.61.1",
    "wrangler": "^4.112.0"
  }
}
