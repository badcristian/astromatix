# Cloudflare Workers static-assets headers.
#
# The site is indexable by default — this file deliberately sets no robots
# response header (see src/lib/seo.ts / FINDINGS.md #38). Kept as the place to
# add response headers (caching, security) at go-live.
